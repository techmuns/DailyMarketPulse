# Production build runs in live mode: the dashboard overlays the real
# feeds (public/data/live.json + moneycontrol.json) and shows Live/Delayed
# data-source chips instead of "Mock". Local `npm run dev` stays mock
# unless you add the same line to .env.development or .env.local.
VITE_DATA_MODE=live
